config = load_config()

# Example usage in your monitoring logic
region = config['AWS_REGION']
sns_topic_arn = config['SNS_TOPIC_ARN']
ec2_instance_limit = config['EC2_INSTANCE_LIMIT']
alert_email = config['ALERT_EMAIL']

print(f"Monitoring EC2 instances in region: {region}")
# Add the rest of your monitoring logic here
