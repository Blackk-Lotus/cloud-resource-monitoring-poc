boto3==1.24.0         # AWS SDK for Python to interact with AWS services
requests==2.26.0      # For making HTTP requests (optional)
numpy==1.21.0         # For numerical operations (if needed)
pandas==1.3.0         # For data manipulation and analysis (if needed)
python-dotenv==0.19.0 # For managing environment variables from a .env file (optional)
